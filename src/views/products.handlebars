

<h2>Productos</h2>
<hr>

<!-- En products.handlebars -->

{{#each products}}
    <div>
        <h2>{{title}}</h2>
        <p>{{description}}</p>
        <p>{{price}}</p>
        <!-- Agrega otras propiedades según sea necesario -->
    </div>
{{/each}}
